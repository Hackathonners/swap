<div class="card-body p-5 text-center">
    <h4 class="card-title"><small>You have no pending exchange requests</small></h4>
    <p class="card-text text-muted">Pending exchange requests will appear here.</p>
    <!-- this should later be a button to the exchange listing>
        <a href="{{-- route('exchangelisting.index') --}}" class="btn btn-info">Exchange a shift</a>
    -->
</div>
